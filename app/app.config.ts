export default defineAppConfig({
  // Nuxt UI configuration (optional, can be expanded as needed)
  ui: {
    colors: {
        primary: 'blue',
        secondary: 'emerald',
        info: 'amber',
        neutral: 'gray',
    }
  }
})
